import { WeightPipePipe } from './weight-pipe.pipe';

describe('WeightPipePipe', () => {
  it('create an instance', () => {
    const pipe = new WeightPipePipe();
    expect(pipe).toBeTruthy();
  });
});
